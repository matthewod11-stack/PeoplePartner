-- Drop insight canvas tables (removed in Launch Prep Phase A)
-- Drop in reverse dependency order
DROP TABLE IF EXISTS chart_annotations;
DROP TABLE IF EXISTS pinned_charts;
DROP TABLE IF EXISTS insight_boards;
